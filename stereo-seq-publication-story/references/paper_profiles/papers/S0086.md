---
paper_id: S0086
title: "Spatial-linked alignment tool (SLAT) for aligning heterogenous slices."
source: paper_skill_matrix.tsv; paper_figures.tsv
profile_type: objective_scan_plus_story_scan
---

# S0086: Spatial-linked alignment tool (SLAT) for aligning heterogenous slices.

Metadata:
- DOI: `10.1038/s41467-023-43105-5`
- Journal/year: Nat Commun, 2023
- Species: `not captured`
- Tissue/system: `not captured`
- Disease/condition captured: `not captured`
- Developmental stage captured: `not captured`
- Full-text status in corpus: `pmc_xml`
- Code/data URLs captured: `https://github.com/gao-lab/SLAT`
- Tools observed: `Seurat:2;Harmony:2;Scanpy:1;pySCENIC:1;GraphST:1`
- Skill tags: `3d_reconstruction:4;spatial_domain:4;cell_type_mapping:3;publication_plotting:3;developmental_trajectory:1`

Objective Corpus Scan:
- The corpus entry describes `Spatial-linked alignment tool (SLAT) for aligning heterogenous slices` in `species not captured` / `tissue not captured`. The captured metadata reports observed analysis tags include 3d_reconstruction, spatial_domain, cell_type_mapping, publication_plotting, developmental_trajectory; observed plot types include umap_tsne, spatial_map, bar_pie, network_chord, heatmap, dotplot_bubble; observed tools include Seurat, Harmony, Scanpy, pySCENIC, GraphST. `paper_figures.tsv` contains 4 main figure captions for this paper.

Observed Components:
- Analysis tags observed by corpus parser: 3d_reconstruction, spatial_domain, cell_type_mapping, publication_plotting, developmental_trajectory.
- Tools/packages detected by corpus parser: Seurat, Harmony, Scanpy, pySCENIC, GraphST.
- Plot types detected by corpus parser: umap_tsne, spatial_map, bar_pie, network_chord, heatmap, dotplot_bubble.

Figure-By-Figure Caption Digest:
- Fig. 1: Architecture of SLAT framework. The SLAT algorithm can be divided into three main steps: model, represent and align. We model omics data from two single-cell spatial slices as low dimensional representations X 1 ∈ R N 1 × M , X 2 ∈ R N 2 × M obtained via SVD-based cross-dataset decomposition, where N 1 , N 2 are cell numbers of each slice and M is the SVD dimensionality. We use spatial coordinates of the cells to build K -neighbor spatial graphs G 1 = ( V 1 , E 1 , X 1 ) , G 2 = ( V 2 , E 2 , X 2 ) in each slice, respectively. The neighbor size can be set dynamically to adjust for uneven spatial resolution across technologies. To encode local and global spatial information, SLAT uses L -layer lightweight graph convolution networks to propagate and aggregate information across the spatial graph into cell embeddings X ^ 1 , X ^ 2 , which are then projected into alignment space Z 1 , Z 2...
  - Observed analysis/plot/tool tags: 3d_reconstruction, spatial_domain, network_chord, spatial_map, umap_tsne.
  - Caption keyword cues: spatial, go, if.
- Fig. 2: Evaluation on homogeneous spatial alignment. a Summary of the three benchmark datasets. b Visualization of alignment results of different methods on the benchmark datasets in ( a ). Slices are colored according to alignment correctness of cell type and spatial region. For example, green means both cell type and region are corrected aligned. Only alignments between the first two slices are shown. PASTE failed to run on the Stereo-seq dataset due to GPU memory overflow (capping at 80 GB). c Heatmaps quantifying the region matching accuracy and cell-type matching accuracy, respectively, in ( b ), in the form of contingency tables. The number in each cell is the average proportion across eight repeats with different random seeds. d Joint accuracy (“Methods”) of different methods on the aggregated datasets. PASTE failed to run on the Stereo-seq dataset due to GPU memory overflow (capping a...
  - Observed analysis/plot/tool tags: 3d_reconstruction, cell_type_mapping, publication_plotting, spatial_domain, bar_pie, heatmap, spatial_map, Seurat, Harmony.
  - Caption keyword cues: spatial, heatmap, if, fish.
- Fig. 3: Spatial alignment across distinct technologies and modalities. a Visualization of the alignment of E8.75 seqFISH mouse embryo and E9.5 Stereo-seq mouse embryo datasets, colored by cell types (subsampled to 300 alignment pairs for clear visualization). b Highlighting the alignment of cells labeled as “Neural crest” in Stereo-seq (top) to seqFISH (bottom), alignment lines are colored by cell type of aligned cells in seqFISH. One of the cells in the head region of Stereo-seq that aligned to the abdomen region of seqFISH is a neural crest cell which migrates throughout the whole embryo. c UMAP visualization of cells labeled as “Neural crest” in Stereo-seq, colored by cell type of aligned cells in seqFISH, cell types with fractions less than 5% are collectively labeled as “Other”. Dashed circles indicate manual annotation by marker genes (corresponding to Supplementary Fig. 7d, e ). d Visu...
  - Observed analysis/plot/tool tags: 3d_reconstruction, cell_type_mapping, publication_plotting, spatial_domain, bar_pie, umap_tsne.
  - Caption keyword cues: umap, spatial, marker, fish.
- Fig. 4: Developmental alignment of mouse embryo. a Visualization of the alignment of E11.5 and E12.5 mouse embryo Stereo-seq datasets, colored by cell types (subsampled to 300 alignment pairs for clear visualization). b Similarity score of the alignment. Higher scores indicate higher alignment confidence. Dashed circles highlight five regions with low similarity scores. c Sanky plot showing cell type correspondence of SLAT alignment between the two datasets. The green box highlights cells labeled as “Kidney” and “Ovary” in E12.5. d Alignment visualization highlighting cells labeled as “Ovary” (left) and “Kidney” (right) in E12.5 and their aligned cells in E11.5, respectively. e UMAP visualization of cell type annotations for cells labeled as “Kidney” in E12.5 and their aligned cells in E11.5. f Dot plot showing marker gene expression of cell types in ( e ). Source data are provided as a Sourc...
  - Observed analysis/plot/tool tags: 3d_reconstruction, cell_type_mapping, developmental_trajectory, publication_plotting, spatial_domain, dotplot_bubble, umap_tsne.
  - Caption keyword cues: umap, marker, dot plot.

Story / Reuse Scan:
- Central question: What analysis capability does this method add for spatial transcriptomics, and how is it validated across datasets?
- Biological gap: inferred from title, figure captions and corpus metadata; verify against the full text before treating it as a claim.
- Technical gap: spatial transcriptomics is used to localize cell states, domains, programs or method outputs in tissue coordinates.
- Archetype: method_or_workflow.
- Main axis: method.
- Final claim type: method benchmark or reusable workflow.

Figure Logic:
- Fig. 1: setup and atlas overview; analyses include 3d_reconstruction, spatial_domain; plot types include network_chord, spatial_map, umap_tsne.
- Fig. 2: cell-type mapping and annotation; analyses include 3d_reconstruction, cell_type_mapping, publication_plotting, spatial_domain; plot types include bar_pie, heatmap, spatial_map.
- Fig. 3: cell-type mapping and annotation; analyses include 3d_reconstruction, cell_type_mapping, publication_plotting, spatial_domain; plot types include bar_pie, umap_tsne.
- Fig. 4: cell-type mapping and annotation; analyses include 3d_reconstruction, cell_type_mapping, developmental_trajectory, publication_plotting, spatial_domain; plot types include dotplot_bubble, umap_tsne.

Detailed Figure Reading:
- Fig. 1: setup and atlas overview. The caption-derived evidence points to signals: spatial, GO, IF. The analysis layer is 3d_reconstruction, spatial_domain, visualized mainly as network_chord, spatial_map, umap_tsne. Story use: this figure supplies the setup and atlas overview step in the paper's evidence chain. For reuse, record the input assumptions, benchmark datasets, comparison methods and failure modes before applying the method to a new tissue.
- Fig. 2: cell-type mapping and annotation. The caption-derived evidence points to signals: spatial, heatmap, IF, FISH. The analysis layer is 3d_reconstruction, cell_type_mapping, publication_plotting, spatial_domain, visualized mainly as bar_pie, heatmap, spatial_map. Story use: this figure supplies the cell-type mapping and annotation step in the paper's evidence chain. For reuse, record the input assumptions, benchmark datasets, comparison methods and failure modes before applying the method to a new tissue.
- Fig. 3: cell-type mapping and annotation. The caption-derived evidence points to signals: UMAP, spatial, marker, FISH. The analysis layer is 3d_reconstruction, cell_type_mapping, publication_plotting, spatial_domain, visualized mainly as bar_pie, umap_tsne. Story use: this figure supplies the cell-type mapping and annotation step in the paper's evidence chain. For reuse, record the input assumptions, benchmark datasets, comparison methods and failure modes before applying the method to a new tissue.
- Fig. 4: cell-type mapping and annotation. The caption-derived evidence points to signals: UMAP, marker, dot plot. The analysis layer is 3d_reconstruction, cell_type_mapping, developmental_trajectory, publication_plotting, spatial_domain, visualized mainly as dotplot_bubble, umap_tsne. Story use: this figure supplies the cell-type mapping and annotation step in the paper's evidence chain. For reuse, record the input assumptions, benchmark datasets, comparison methods and failure modes before applying the method to a new tissue.

Open Method Modules:
- `method_benchmark_and_transfer`: method module; compare against baseline tools, inspect robustness across datasets and document assumptions.
- `3d_or_multislice_reconstruction`: 3d_structure module; tools/signals include Seurat, Harmony, Scanpy, pySCENIC, GraphST; supports the paper's `3d_reconstruction` evidence.
- `spatial_domain_annotation`: spatial_structure module; tools/signals include Seurat, Harmony, Scanpy, pySCENIC, GraphST; supports the paper's `spatial_domain` evidence.
- `cell_type_annotation`: annotation module; tools/signals include Seurat, Harmony, Scanpy, pySCENIC, GraphST; supports the paper's `cell_type_mapping` evidence.
- `paper_figure_assembly`: visualization module; tools/signals include Seurat, Harmony, Scanpy, pySCENIC, GraphST; supports the paper's `publication_plotting` evidence.
- `trajectory_state_transition`: trajectory module; tools/signals include Seurat, Harmony, Scanpy, pySCENIC, GraphST; supports the paper's `developmental_trajectory` evidence.

Reusable Pattern:
- Use this paper as a `method_or_workflow` reference when a new dataset has a matching `method` axis and similar evidence layers.
- Minimal evidence chain: data/QC overview -> spatial/domain or cell-state annotation -> marker/program support -> spatial interpretation -> validation or caveat.
- Do not transfer claims that require unavailable axes, such as condition effects without groups, trajectory without ordered stages, CCI without spatial proximity, or mechanism without validation.

Retrieval Cues:
- Title words: Spatial, linked, alignment, tool, SLAT, for, aligning, heterogenous, slices.
- Corpus tags: 3d_reconstruction:4;spatial_domain:4;cell_type_mapping:3;publication_plotting:3;developmental_trajectory:1.
- Tools: Seurat:2;Harmony:2;Scanpy:1;pySCENIC:1;GraphST:1.

Use Notes:
- This profile is generated from corpus metadata and figure-caption-derived signals.
- Verify sample design, methods, thresholds, code availability, and validation experiments against the full text before using this paper as evidence or a code template.

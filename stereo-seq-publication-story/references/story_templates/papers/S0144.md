---
paper_id: S0144
title: "Single-cell multiome reveals root hair-specific responses to salt stress."
source: paper_skill_matrix.tsv; paper_figures.tsv
review_status: draft_caption_metadata_inferred
---

# S0144: Single-cell multiome reveals root hair-specific responses to salt stress.

Metadata:
- Title: `Single-cell multiome reveals root hair-specific responses to salt stress.`
- Journal/year/DOI: New Phytol, 2025, `10.1111/nph.70160`
- Species/tissue: `not captured`; `root`
- Code/data URLs captured in corpus: `not captured`
- Tools observed: `pySCENIC:2;Seurat:1;Scanpy:1`
- Skill tags: `publication_plotting:11;cell_type_mapping:11;spatial_domain:7;spatial_grn_regulon:4;quality_control_preprocessing:3;spatial_programs:3;developmental_trajectory:3`

Story layer:
- Central question: How does the condition reshape spatial domains, cell states and functional programs?
- Biological gap: inferred from title, figure captions and corpus metadata; requires full-text audit before becoming prescriptive skill behavior.
- Technical gap: spatial transcriptomics is used to localize cell states, domains, programs or method outputs in tissue coordinates.
- One-sentence story: Single-cell multiome reveals root hair-specific responses to salt stress is treated here as a `disease_or_condition;plant_spatial_atlas` template organized around `condition;organ`.
- Archetype: disease_or_condition;plant_spatial_atlas.
- Main axis: condition;organ.
- Final claim type: condition-associated spatial atlas and candidate mechanisms.

Figure logic:
- No main figure captions were captured in `paper_figures.tsv`; audit full text before using this template.

Detailed figure reading:
- Figure-level reading is unavailable from the current corpus tables.

Open method modules:
- `paper_figure_assembly`: visualization module; tools/signals include pySCENIC, Seurat, Scanpy; supports the paper's `publication_plotting` evidence.
- `cell_type_annotation`: annotation module; tools/signals include pySCENIC, Seurat, Scanpy; supports the paper's `cell_type_mapping` evidence.
- `spatial_domain_annotation`: spatial_structure module; tools/signals include pySCENIC, Seurat, Scanpy; supports the paper's `spatial_domain` evidence.
- `grn_regulon_activity`: regulatory_network module; tools/signals include pySCENIC, Seurat, Scanpy; supports the paper's `spatial_grn_regulon` evidence.
- `qc_preprocessing`: preprocessing module; tools/signals include pySCENIC, Seurat, Scanpy; supports the paper's `quality_control_preprocessing` evidence.
- `deg_program_pathway_analysis`: differential_program module; tools/signals include pySCENIC, Seurat, Scanpy; supports the paper's `spatial_programs` evidence.
- `trajectory_state_transition`: trajectory module; tools/signals include pySCENIC, Seurat, Scanpy; supports the paper's `developmental_trajectory` evidence.

Reusable pattern:
- Use this paper as a `disease_or_condition;plant_spatial_atlas` template when a new dataset has a matching `condition;organ` axis and similar evidence layers.
- Minimal evidence chain: data/QC overview -> spatial/domain or cell-state annotation -> marker/program support -> spatial interpretation -> validation or caveat.
- Do not transfer claims that require unavailable axes, such as condition effects without groups, trajectory without ordered stages, CCI without spatial proximity, or mechanism without validation.

Audit notes:
- This draft was generated from corpus metadata and figure-caption-derived signals, not a full manual methods audit.
- Before using as skill behavior, verify sample design, exact tools, thresholds, code availability and whether validation experiments support the claimed biology.

interface:
  display_name: "Stereo-seq 3D Visualization"
  short_description: "Render standalone dark 3D ST figures, GIFs, and slice maps"
  default_prompt: "Use $stereo-seq-3d-visualization to create standalone black-background PNG/PDF 3D Stereo-seq snapshots, PNG/PDF contact sheets, optional rotation GIFs, and single-slice maps using SPACEL/Scube-backed scripts. Do not create HTML webpages unless I explicitly ask for interactive HTML."
policy:
  allow_implicit_invocation: true
